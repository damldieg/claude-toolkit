# Project State

Not a changelog and not a status report — git log and the code already cover "what changed." This file exists only for the "why" that isn't recoverable from either: active decisions, discarded approaches, and the next known step. Hard cap: 1-2 screens. At that size, prune stale entries instead of archiving the whole file.

Update only at a completed milestone, a material decision, or when `/ctk-checkpoint` is requested — not after every small task.

## Current state

<1-2 lines: where the project stands right now.>

## Active decisions

- <decision> — <short reason> (<date>)

## Discarded and why

- <approach considered but not taken> — <one-line reason, so it doesn't get re-proposed>

## Next known step

<if there is one; omit the section if not>
